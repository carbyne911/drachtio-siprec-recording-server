The build.txt is a description file of how the build.yml file should look like originally if wished to be used.
The build.yml would be used by github to run a CI flow according to the tasks described.